# AGENTS.md

## Cursor Cloud specific instructions

This repository is a scratch/placeholder workspace. As of the last analysis:

- **No application code** exists on `main` or the current working branch.
- **No dependencies**, package managers, frameworks, or build systems are configured.
- **No tests, linting, or build commands** are available.
- The only file on `main` is `23423` (contains "hola").
- Other remote branches contain isolated CTF (Capture The Flag) challenge artifacts (reverse engineering, recon, etc.) that are not part of a cohesive product.

If application code is added in the future, this file should be updated with relevant setup, build, test, and run instructions.
